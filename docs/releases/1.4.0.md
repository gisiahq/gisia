# Release of 1.4.0

## What's new

1. Add namespace runners feature
2. Add group members
3. Add group API
4. Show authorized projects on the homepage
5. Add "new subgroup" button
6. Enhance the namespace options
7. Fix maintainer runner page showing the wrong text
8. Fix the pin icon bounce issue
9. Fix project member max role error
10. Fix project list menu position

## Actions

1. Change the gisia version to `image: gisia/gisia:1.4.0` in your `docker-compose.yml`
2. Run `docker compose up -d` to update the web service
