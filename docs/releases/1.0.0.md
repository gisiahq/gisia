# Release of 1.0.0

## What's new

1. Add merge API
2. Add remove source branch after merge setting
5. Support paste to upload attachments
6. Hide normal notes input by default

## Actions

1. Change the gisia version to `image: gisia/gisia:1.0.0` in your `docker-compose.yml`
