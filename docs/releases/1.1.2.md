# Release of 1.1.2

## What's new

1. use ENV['GISIA_HOST'] as the default ssh host
2. Update thread config
3. Add dev profiler tool

## Actions

1. Change the gisia version to `image: gisia/gisia:1.1.2` in your `docker-compose.yml`
