# Release of 1.4.1

## What's new

1. Add frontend translation support with gettext generation
2. Run pipelines on merge requests
3. Fix reset jobs
4. Fix retry jobs
5. Fix translations

## Actions

1. Change the gisia version to `image: gisia/gisia:1.4.1` in your `docker-compose.yml`
2. Run `docker compose up -d` to update the web service
