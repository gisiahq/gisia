# Release of 1.1.0

## What's new

1. Add Git LFS support
2. Scoped CI job token within the project

## Actions

1. Change the gisia version to `image: gisia/gisia:1.1.0` in your `docker-compose.yml`
