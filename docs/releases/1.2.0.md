# Release of 1.2.0

## What's new

1. Add clear (x) button to assignees, labels, epics, epic assignees, and merge requests
2. Sort and fix the order of pipeline jobs
3. Fix member role update permission check (including the member API)
4. Fix flash message not showing on the pipeline trigger page

## Actions

1. Change the gisia version to `image: gisia/gisia:1.2.0` in your `docker-compose.yml`
