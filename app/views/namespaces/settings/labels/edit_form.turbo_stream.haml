= turbo_stream.replace 'labels-content' do
  = turbo_frame_tag 'labels-content' do
    = render 'shared/labels/form', label: @label, url: namespace_label_form_url(@namespace, @label), cancel_path: namespace_settings_labels_path(@namespace.full_path)

= turbo_stream.replace 'flash' do
  %div#flash{ class: 'fixed top-0 left-64 right-0 z-21' }
